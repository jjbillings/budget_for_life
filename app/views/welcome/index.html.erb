<h1>Budget For Life</h1>
<h3>Save like a nerd</h3>
<div class="container">
  <table class="table">
    <tr>
      <td><%= button_to "View All Users", users_path, { method: "get", class: "btn btn-primary" } %></td>
      <td><%= button_to "View All Savings Goals", savings_goals_path, { method: "get", class: "btn btn-primary" } %></td>
    </tr>
    <tr>
      <td><%= button_to "Create User", new_user_path, { method: "get", class: "btn btn-primary" } %></td>
      <td><%= button_to "Create Savings Goal", new_savings_goal_path, { method: "get", class: "btn btn-primary" } %></td>
    </tr>
  </table>
</div>
